{
  "name": "holiday-timeline",
  "version": "1.0.0",
  "description": "Jira 프로젝트용 자체 타임라인 + 구글 캘린더 한국 공휴일 빨간 표시 Forge 앱",
  "scripts": {
    "build:ui": "npm --prefix static/timeline install && npm --prefix static/timeline run build",
    "install:all": "npm install && npm --prefix static/timeline install"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0"
  }
}
